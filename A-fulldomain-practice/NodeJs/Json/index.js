const data=require('./data.json')
// json data is converted to javascript object
console.log(data.name)


// watch mode is a feature where we can track any changes occured in a file will be noticed 
// node --watch filename

// console.log('watch  on');
